# Multi-Agent Spatial Conditioning - Requirements
# ================================================

# Core dependencies
python-dotenv>=1.0.0
numpy>=1.21.0
httpx>=0.24.0
aiofiles>=23.0.0
Pillow>=9.0.0

# Fal.ai client for video generation
fal-client>=0.4.0

# Replicate cloud I2V fallback (Fase 3.5)
replicate>=0.25.0

# OpenCV for video processing and skeleton detection
opencv-python>=4.8.0

# FFmpeg Python bindings for dynamic retriever
ffmpeg-python>=0.2.0

# MediaPipe Pose for canvas padding autodetect (Fase 3.14)
mediapipe>=0.10.0

# Optional: ControlNet auxiliary for OpenPose detection
# Install with: pip install controlnet-aux
# For GPU support: pip install controlnet-aux[gpu]
# controlnet-aux>=0.0.7

# Optional: PyTorch (required by controlnet-aux)
# Install separately based on your CUDA version:
# CPU only: pip install torch torchvision
# CUDA 11.8: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# CUDA 12.1: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# torch>=2.0.0
# torchvision>=0.15.0

# Optional: InsightFace for real identity embeddings (recommended)
# pip install insightface onnxruntime
# insightface>=0.7.3
# onnxruntime>=1.16.0
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# black>=23.0.0
# mypy>=1.0.0
